import {render} from "react-testing-library";
import SectionSwitcher from "../SectionSwitcher";

describe("<SectionSwitcher /> switches between sections and generates sections by an array with objects", () => {
  test("should be able to render", () => {});
  test("should be able to generate the right amount of sections", () => {});

  test("should be able to select a default section", () => {});
});
